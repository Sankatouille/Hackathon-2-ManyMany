{% extends 'base.html.twig' %}

{% block title %}Salle de bain - catégories{% endblock %}

{% block body %}


    <h1>Salle de bain - Les catégories</h1>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.id }}</td>
                <td>{{ categorie.nom }}</td>
                <td>
                    {# <a href="{{ path('categorie_show', {'id': categorie.id}) }}">show</a>
                    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">edit</a> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <center>
        <div class="btn">
            <a href="{{ path('categorie_index') }}">Retour</a>
        </div>
    </center>

    <a href="{{ path('categorie_new') }}">Create new</a>
{% endblock %}
